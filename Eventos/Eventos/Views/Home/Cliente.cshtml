@model Eventos.Models.Login
@{
    ViewBag.Title = "Cliente";
}
<h2>@ViewBag.Title</h2>
@using (Html.BeginForm("MenuCliente", "Home", FormMethod.Post))
{
    <div>
        <table>
            <tr>
                <td>
                    Usuario:
                    @Html.EditorFor(model => model.usuario, new { htmlAttributes = new { @class = "form-control" } })
                    <br />
                    Contrasena:
                    @Html.EditorFor(model => model.contrasena, new { htmlAttributes = new { @class = "form-control" } })
                    <br />
                    <input type="submit" value="Login" />
                    @Html.ActionLink("Registrar", "RegistrarCliente")

                </td>
            </tr>
        </table>
    </div>
}